# LiteMonitor 快捷键功能实现方案

## 📋 项目现状分析

### 当前键盘支持情况
- **零键盘支持**：项目目前没有任何键盘快捷键功能
- **无全局热键**：没有使用 `RegisterHotKey`、`GlobalHook` 等系统级热键
- **无菜单快捷键**：菜单项没有设置 `ShortcutKeys` 属性
- **无键盘事件**：没有 `KeyDown`、`KeyPress`、`KeyUp` 事件处理

### 技术栈
- **平台**：Windows Forms (.NET 8.0)
- **架构**：MVVM-like 模式，清晰的 Settings 系统和 AppActions
- **依赖**：仅 LibreHardwareMonitorLib 和 System.Text.Json

## 🎯 实现方案选择：混合方案

结合两种技术实现不同场景的快捷键需求：

1. **全局系统热键**：用于程序最小化或后台时仍需响应的功能
2. **应用程序级快捷键**：用于程序窗口激活状态下的界面操作

## 🔧 技术实现架构

### 1. 配置数据结构扩展

```csharp
// 在 Settings.cs 中添加
public enum ModifierKeys
{
    None = 0,
    Ctrl = 1,
    Alt = 2,
    Shift = 4,
    Win = 8
}

public class HotkeyConfig
{
    public string ActionId { get; set; } = "";           // 动作标识符
    public string Description { get; set; } = "";        // 功能描述
    public bool Enabled { get; set; } = true;           // 是否启用
    public Keys Key { get; set; } = Keys.None;          // 主键
    public ModifierKeys Modifiers { get; set; } = ModifierKeys.None; // 修饰键
    public bool IsGlobal { get; set; } = false;         // 是否为全局热键
    public bool RequireAdmin { get; set; } = false;     // 是否需要管理员权限
}

// 在 Settings 类中添加
public List<HotkeyConfig> Hotkeys { get; set; } = new List<HotkeyConfig>();
```

### 2. 热键管理器核心类

```csharp
public class HotkeyManager : IDisposable
{
    private readonly Settings _cfg;
    private readonly MainForm _mainForm;
    private readonly Dictionary<int, HotkeyConfig> _registeredHotkeys = new();
    private int _nextHotkeyId = 0x0000;
    
    // Windows API 声明
    [DllImport("user32.dll")]
    private static extern bool RegisterHotKey(IntPtr hWnd, int id, uint fsModifiers, uint vk);
    
    [DllImport("user32.dll")]
    private static extern bool UnregisterHotKey(IntPtr hWnd, int id);
    
    public HotkeyManager(Settings cfg, MainForm mainForm)
    {
        _cfg = cfg;
        _mainForm = mainForm;
    }
    
    public void Initialize()
    {
        // 注册全局热键
        RegisterGlobalHotkeys();
        
        // 设置应用级键盘事件
        _mainForm.KeyPreview = true;
        _mainForm.KeyDown += OnAppKeyDown;
    }
    
    private void RegisterGlobalHotkeys()
    {
        foreach (var hotkey in _cfg.Hotkeys.Where(h => h.Enabled && h.IsGlobal))
        {
            try
            {
                uint modifiers = ConvertModifiers(hotkey.Modifiers);
                uint vk = (uint)hotkey.Key;
                
                if (RegisterHotKey(_mainForm.Handle, _nextHotkeyId, modifiers, vk))
                {
                    _registeredHotkeys[_nextHotkeyId] = hotkey;
                    _nextHotkeyId++;
                }
            }
            catch (Exception ex)
            {
                // 记录热键注册失败
                Logger.Warn($"Failed to register hotkey {hotkey.ActionId}: {ex.Message}");
            }
        }
    }
    
    // 处理 Windows 消息
    protected override void WndProc(ref Message m)
    {
        const int WM_HOTKEY = 0x0312;
        
        if (m.Msg == WM_HOTKEY)
        {
            int id = m.WParam.ToInt32();
            if (_registeredHotkeys.TryGetValue(id, out var hotkey))
            {
                ExecuteAction(hotkey.ActionId);
            }
        }
        
        base.WndProc(ref m);
    }
    
    private void OnAppKeyDown(object sender, KeyEventArgs e)
    {
        // 处理应用级快捷键
        var pressedModifiers = GetPressedModifiers();
        var pressedKey = e.KeyCode;
        
        var hotkey = _cfg.Hotkeys.FirstOrDefault(h => 
            h.Enabled && !h.IsGlobal && 
            h.Key == pressedKey && 
            h.Modifiers == pressedModifiers);
            
        if (hotkey != null)
        {
            ExecuteAction(hotkey.ActionId);
            e.Handled = true;
        }
    }
    
    public void ExecuteAction(string actionId)
    {
        // 映射到具体的 AppActions
        switch (actionId)
        {
            // 这里根据你的选择填充具体动作
        }
    }
}
```

## 📋 可选的快捷键功能列表

以下是 LiteMonitor 所有可能需要的快捷键功能，请根据实际需求筛选：

### 🖥️ 窗口控制类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 显示/隐藏主窗口 | `ToggleVisibility` | `F12` | ✅ | 快速显示或隐藏监控窗口 |
| 切换置顶状态 | `ToggleTopMost` | `Ctrl+T` | ❌ | 切换窗口置顶状态 |
| 切换鼠标穿透 | `ToggleClickThrough` | `Ctrl+P` | ❌ | 切换鼠标穿透模式 |
| 切换自动隐藏 | `ToggleAutoHide` | `Ctrl+H` | ❌ | 切换靠边自动隐藏 |
| 限制拖出屏幕 | `ToggleClampToScreen` | `Ctrl+L` | ❌ | 切换限制拖出屏幕 |

### 🎨 显示模式类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 切换竖屏/横屏模式 | `ToggleLayoutMode` | `F5` | ❌ | 切换显示布局模式 |
| 切换任务栏显示 | `ToggleTaskbar` | `F11` | ✅ | 显示/隐藏任务栏监控 |
| 增加透明度 | `IncreaseOpacity` | `Ctrl+Up` | ❌ | 增加窗口透明度 |
| 减少透明度 | `DecreaseOpacity` | `Ctrl+Down` | ❌ | 减少窗口透明度 |
| 重置透明度 | `ResetOpacity` | `Ctrl+0` | ❌ | 重置为默认透明度 |

### 📊 监控项控制类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 切换CPU显示 | `ToggleCpuDisplay` | `Ctrl+1` | ❌ | 显示/隐藏CPU监控 |
| 切换GPU显示 | `ToggleGpuDisplay` | `Ctrl+2` | ❌ | 显示/隐藏GPU监控 |
| 切换内存显示 | `ToggleMemoryDisplay` | `Ctrl+3` | ❌ | 显示/隐藏内存监控 |
| 切换磁盘显示 | `ToggleDiskDisplay` | `Ctrl+4` | ❌ | 显示/隐藏磁盘监控 |
| 切换网络显示 | `ToggleNetworkDisplay` | `Ctrl+5` | ❌ | 显示/隐藏网络监控 |
| 切换所有监控 | `ToggleAllMonitors` | `Ctrl+A` | ❌ | 显示/隐藏所有监控项 |

### 🔧 工具功能类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 打开设置窗口 | `OpenSettings` | `Ctrl+S` | ❌ | 打开程序设置 |
| 打开流量统计 | `OpenTrafficHistory` | `Ctrl+N` | ❌ | 打开流量统计窗口 |
| 打开硬件信息 | `OpenHardwareInfo` | `Ctrl+I` | ❌ | 打开硬件信息面板 |
| 清理内存 | `CleanMemory` | `Ctrl+M` | ❌ | 执行内存清理 |
| 网络测速 | `SpeedTest` | `Ctrl+B` | ❌ | 执行网络测速 |
| 打开关于窗口 | `OpenAbout` | `F1` | ❌ | 打开关于窗口 |

### 📸 截图与记录类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 截取监控界面 | `Screenshot` | `Ctrl+F10` | ✅ | 截取当前监控界面 |
| 开始/停止记录 | `ToggleRecording` | `Ctrl+R` | ✅ | 开始/停止性能记录 |
| 保存当前状态 | `SaveSnapshot` | `Ctrl+Shift+S` | ❌ | 保存当前监控状态快照 |

### ⚙️ 系统功能类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 打开任务管理器 | `OpenTaskManager` | `Ctrl+Shift+Esc` | ✅ | 打开系统任务管理器 |
| 重启监控服务 | `RestartMonitor` | `Ctrl+Shift+R` | ❌ | 重启硬件监控服务 |
| 检查更新 | `CheckUpdate` | `Ctrl+U` | ❌ | 手动检查程序更新 |
| 退出程序 | `ExitApplication` | `Ctrl+Q` | ❌ | 退出LiteMonitor |
| 重启程序 | `RestartApplication` | `Ctrl+Shift+Q` | ❌ | 重启LiteMonitor |

### 🎚️ 数值调整类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 增加刷新频率 | `IncreaseRefreshRate` | `Ctrl+]` | ❌ | 增加监控刷新频率 |
| 减少刷新频率 | `DecreaseRefreshRate` | `Ctrl+[` | ❌ | 减少监控刷新频率 |
| 增加UI缩放 | `IncreaseUIScale` | `Ctrl+Plus` | ❌ | 增加界面缩放比例 |
| 减少UI缩放 | `DecreaseUIScale` | `Ctrl+Minus` | ❌ | 减少界面缩放比例 |
| 重置UI缩放 | `ResetUIScale` | `Ctrl+Shift+0` | ❌ | 重置界面缩放比例 |

### 🎯 快速操作类
| 功能 | 动作ID | 建议快捷键 | 全局热键 | 描述 |
|------|--------|------------|----------|------|
| 显示/隐藏系统托盘图标 | `ToggleTrayIcon` | `Ctrl+Y` | ❌ | 切换系统托盘图标显示 |
| 切换主题 | `ToggleTheme` | `F9` | ❌ | 循环切换主题 |
| 切换语言 | `ToggleLanguage` | `Ctrl+L` | ❌ | 循环切换界面语言 |
| 显示快捷键帮助 | `ShowHotkeyHelp` | `F2` | ❌ | 显示快捷键帮助窗口 |

## 🚀 实施步骤

### 阶段一：基础框架（1-2天）
1. 扩展 `Settings` 类添加热键配置结构
2. 创建 `HotkeyManager` 基础类
3. 实现应用级快捷键支持（ProcessCmdKey）
4. 添加默认的5-8个核心快捷键

### 阶段二：系统热键（2-3天）
1. 实现 Windows API `RegisterHotKey`/`UnregisterHotKey`
2. 添加全局热键冲突检测和处理
3. 完善热键管理器生命周期管理
4. 测试不同Windows版本的兼容性

### 阶段三：设置界面（2-3天）
1. 在 `SettingsForm` 中添加"快捷键"配置标签页
2. 实现热键录制和显示控件
3. 添加热键冲突检测和用户提示
4. 支持快捷键配置的导入/导出功能

### 阶段四：优化完善（1-2天）
1. 多语言支持（快捷键描述翻译）
2. 错误处理和详细日志记录
3. 性能优化（避免频繁的配置读写）
4. 用户文档和帮助系统集成

## ⚠️ 注意事项

### 技术注意事项
1. **权限问题**：全局热键在部分系统可能需要管理员权限
2. **热键冲突**：需要检测系统已注册的热键并提示用户
3. **多显示器支持**：确保热键在所有显示器场景下正常工作
4. **系统休眠恢复**：处理系统休眠/唤醒后的热键重新注册
5. **64位系统兼容**：确保P/Invoke调用在64位系统正常工作

### 用户体验注意事项
1. **默认快捷键选择**：避免与常用软件（浏览器、编辑器）冲突
2. **可发现性**：在菜单中显示当前设置的快捷键
3. **可恢复性**：提供重置为默认快捷键的功能
4. **无障碍访问**：考虑键盘导航和屏幕阅读器兼容性
5. **学习曲线**：提供快捷键帮助页面或提示

### 性能注意事项
1. **热键响应延迟**：确保热键响应在50ms以内
2. **内存占用**：热键管理器应轻量，避免内存泄漏
3. **CPU使用率**：键盘事件处理不应明显增加CPU负担
4. **配置读写**：避免频繁的配置文件读写操作

## 📊 推荐的核心功能优先级

### 高优先级（建议首批实现）
1. `ToggleVisibility` - 显示/隐藏主窗口（全局）
2. `Screenshot` - 截取监控界面（全局）
3. `OpenSettings` - 打开设置窗口
4. `ToggleLayoutMode` - 切换显示模式
5. `ToggleTaskbar` - 切换任务栏显示

### 中优先级（第二批实现）
6. `CleanMemory` - 清理内存
7. `OpenTrafficHistory` - 打开流量统计
8. `ToggleTopMost` - 切换置顶状态
9. `SpeedTest` - 网络测速
10. `ToggleAllMonitors` - 切换所有监控

### 低优先级（后续完善）
11. 各个监控项的独立开关
12. 透明度调整快捷键
13. 刷新频率调整
14. 主题切换快捷键
15. 高级截图和记录功能

## 🔍 下一步行动

请根据实际需求从上述功能列表中选择需要实现的快捷键，然后我们可以：

1. **确定首批实现的功能**（选择5-10个最常用的）
2. **设计默认快捷键组合**（避免与系统和其他软件冲突）
3. **创建详细的技术实现计划**
4. **开始编码实现**

你可以在本文件中标记需要实现的功能，然后我将开始具体的代码实现工作。

---
*最后更新：2025年1月24日*
*方案设计：Sisyphus AI Agent*